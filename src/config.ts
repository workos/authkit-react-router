// Re-export configuration from authkit-session
export { configure, getConfig, validateConfig } from '@workos/authkit-session';
export type { AuthKitConfig, ValueSource } from '@workos/authkit-session';
